pipeline {
    agent any
    tools{
        maven 'M2_HOME'
    }
    stages{
        stage('all maven commands to add here'){
            steps{
                sh 'mvn clean test compile install package'
            }
        }
    }
}